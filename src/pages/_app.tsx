import '../style/global.css';

import {challengeContext, ChallengesProvider} from '../context/challengeContext';
import { useState } from 'react';

function MyApp({ Component, pageProps }) {
  

  return (
    
      <Component {...pageProps} />

  )
}


export default MyApp
